$('#doctors').append("<%= j render('records/rend_doctors', locals: { :@doctors => @doctors }) %>");

$( "#selectable1" ).selectable({
      stop: function() {
        var doctorid = $('#selectable1 .ui-selected').attr('data-doctorid');



        // alert(doctorid);

        // $('.day.ui-selected').removeClass("ui-selected");



        // $.ajax({
        //   url: "/records/get_doctors",
        //   data: { specialization: spec},
        //   dataType: 'script'
        // });

        // $('#doctors').empty();
        // var result = $( "#select-result-spec-category" ).empty();
        // $( ".ui-selected", this ).each(function() {
        //   var index = $( "#selectable1 li" ).index( this );
        //   result.append( " #" + ( index + 1 ) );
        // });
      }
    });
